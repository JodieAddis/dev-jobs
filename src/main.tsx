import React from "react";
import ReactDOM from "react-dom/client";
import "../public/sass/style.scss";
import PageDevJob from "./page/PageDevJob";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <PageDevJob />
  </React.StrictMode>
);
